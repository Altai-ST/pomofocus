import React from "react";
const initialState ={
    times = []
}

export const TimeManage =(state = initialState, action)=>{
        switch(action){
            case SWITCH_TIMER:
                 
        }
}